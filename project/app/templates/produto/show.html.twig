{% extends 'base.html.twig' %}

{% block title %}Produto{% endblock %}

{% block body %}
    <h1>Produto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Imagem</th>
                <td>
                    {% if produto.foto is defined %}
                        <img src="{{ asset('upload/produto/'~produto.foto.filename) }}" alt="">
                    {%endif%}
                </td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ produto.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ produto.nome }}</td>
            </tr>
            <tr>
                <th>Descricao</th>
                <td>{{ produto.descricao }}</td>
            </tr>
            <tr>
                <th>Preco</th>
                <td>{{ produto.preco }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_produto_index') }}">back to list</a>

    <a href="{{ path('app_produto_edit', {'id': produto.id}) }}">edit</a>

    {{ include('produto/_delete_form.html.twig') }}
{% endblock %}
